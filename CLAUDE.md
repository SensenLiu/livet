# Life Crutch — Project Context for Claude

> **本文档是新 agent 进入本项目时的入口。请先完整读完本文档，再根据用户指令开展工作。**

## 这是什么项目？

代号 **Life Crutch（暂定中文：「在场」/「Moment」）**，使命是「**让 AI 像拐杖一样伴随每个人一生，真正实现 AI 平权**」。

具体形态：一款多场景"重要时刻 AI 智囊"通用 App + 蓝牙耳机/麦克风组合，用户在自己定义的"重要时刻"一键启动，AI 持续监听、被动提示、跨场景长期记忆、永不让用户重复解释自己。

发起人状态：**个人探索/兴趣阶段**，目前无团队无资金。

## 必读：四条产品哲学铁律（不可违反）

> **「让你不必再向 AI 解释你自己。」**

派生准则（违反任何一条都不该上线）：

1. **AI 主动获取上下文，用户只说"当下"** — AI 已经通过历史对话/复盘/偏好知道你的背景。用户只需说"那个事我该怎么决定"，AI 自动 RAG 检索相关历史。要求底层有长期记忆引擎（Memory Engine）。
2. **被动提示，不期待回应** — 重要场合 AI 不开口对话，而是耳机轻提示音 / 手机字幕弹屏 / 腕带震动。用户可完全忽略。**绝不做"AI 主动对话/插话"**。
3. **数据本地优先，永不保留原声** — 所有用户数据默认存本地 SQLite，云端只有极薄网关。持续监听只转写为文字摘要后销毁原声。这是写进首屏的品牌承诺。
4. **场景识别渐进自动化** — V1 用户手动选场景 → V1.5 已选场景内 Agent 自动切 skill → V2 默认全自动 → V3 24h 全局 Agent。

详见 [`docs/01-philosophy.md`](./docs/01-philosophy.md)。

## 核心战略决策（在多轮深度对话中沉淀，不要再翻案）

| # | 决策 | 关键判断 |
|---|---|---|
| 1 | **不切适老化作为唯一入口** | 个人探索阶段做适老化渠道太重；改为多场景通用 App，让用户行为告诉我们最值得深挖的 vertical |
| 2 | **不做反诈作为独立场景包** | 国内反诈红海且非 LLM 差异化，作为底层语义风险检测能力默默兜底即可 |
| 3 | **MVP = App + 通用蓝牙耳机**，不做小程序，也不自研硬件 | 小程序无法承载"被动感知+主动提示"产品独特性；自研硬件门槛过高 |
| 4 | **端为主架构 + 极薄 BFF 网关（~200 行）** | 不需要重型后端，节省成本同时强化"数据本地"叙事 |
| 5 | **三种交互模式**：🟢主动询问 / 🟡**被动提示**(V1 核心) / 🔴守护监听(V2) | "被动提示"是产品魂，绝不是"AI 主动对话" |
| 6 | **首批 4 个场景包**：S1 看病 / S2 谈话 / S5 情绪 / S8 日复盘 | 覆盖中老年/职场/通用三大类，合规风险低 |
| 7 | **VAD + 前台服务方案**做持续监听 | iPhone/Android 合规 + 续航 10–14 小时 |
| 8 | **GTM = 7 组场景化短视频，不做通用叙事** | 通用 App 的冷启动叙事必须按场景拆 |
| 9 | **路径 C：平台化 + 先切高付费 vertical** | 短期不直接做适老化，但底层能力为"AI 平权"大愿景留口子 |

## 文档导航

```
life_crutch/
├── CLAUDE.md                       ← 你正在读的文件（项目总入口）
├── README.md                       ← 人类可读的项目简介
└── docs/
    ├── 00-product-strategy.md      ← 完整商业策划+研发路线（v3 plan，最权威）
    ├── 01-philosophy.md            ← Part 0 产品哲学铁律（独立精简版）
    ├── 02-decisions-log.md         ← 重要决策记录（含每个决策的反对意见和最终判断）
    ├── 03-open-questions.md        ← 9 个待决开放问题
    ├── 04-next-step-brief.md       ← 给新 agent 的下一步指令（产品/开发方案设计）
    └── user-research/              ← 用户访谈记录（Day 1 之后填）
```

## 当前阶段与下一步

**当前阶段**：策略已定稿（v3），项目目录刚搭起骨架。**没写任何代码**。

**下一步预期**（用户的明确请求）：在另一个新 agent 会话中，基于本文档与 `docs/`，设计：
1. **产品设计方案**（产品具体形态、用户交互流程、关键页面、场景包详细设计、长期记忆引擎设计）
2. **开发方案**（技术栈细化、模块划分、开发计划与里程碑、关键技术点的 PoC 路径）

**新 agent 必须遵循的工作原则**：
- 先读完 `CLAUDE.md` + `01-philosophy.md` + `00-product-strategy.md`
- 任何产品设计**必须遵守 4 条产品哲学铁律**
- 任何架构选择**必须保持"端为主"原则**
- 不要重新讨论"是否做适老化""是否做反诈""是否用小程序 MVP"等已决问题（见 `02-decisions-log.md`）
- 9 个开放问题（见 `03-open-questions.md`）可以推进，但**不要替用户做拍板**
- 用户身份：个人探索阶段，没有团队没有资金，方案要务实可落地

## 数据来源说明

战略与调研内容基于训练知识（截止 2026-01）+ 商业逻辑推演撰写。所有标记 `〔需核对〕` 的数字（如各产品销量、MAU、政策细节）在做投资决策前**务必二次核对**。撰写时网关侧 WebSearch 持续报错，未能补足实时数据。

## 用户行事风格（提醒新 agent）

发起人在多轮对话中表现出以下特征，新 agent 应同样回应：
- **思维缜密、爱挑战假设**：每次都会精准戳中你设计中偷懒的地方
- **不喜欢套话/客套**：直接说观点和依据
- **重视产品哲学**：宁可放慢也要把灵魂校准
- **务实**：明确自己个人探索阶段，方案要"今天就能做"
- **不怕被否定**：你大胆质疑他的判断，他会认真回应

